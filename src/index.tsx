import React from 'react';
import { View } from 'react-native';
import TDDashboardComponent from 'term-deposit-component/src/components/dashboard';

export type TermDepositComponentProps = {};

export type TermDepositComponentStyles = {};

export const TermDepositComponent = (props: TermDepositComponentProps) => {
  return <TDDashboardComponent />;
};

export default TermDepositComponent;
